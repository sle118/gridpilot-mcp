# COM adapter notes

This project is part of **GridPilot MCP**.

For now, the code-level project name remains `ExcelMcp.ComAdapter` to avoid a premature whole-solution rename. This project should eventually contain the only concrete implementation code that talks directly to Excel COM.
